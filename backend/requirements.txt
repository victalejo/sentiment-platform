# Backend dependencies — sentiment-platform
# Generado durante AI Battle (antes no existía requirements.txt).
# Rangos compatibles con las versiones probadas en Python 3.12.

# --- Web framework ---
fastapi>=0.115,<0.116
uvicorn>=0.32,<0.33
python-multipart>=0.0.9        # requerido por OAuth2PasswordRequestForm

# --- ORM / validación ---
SQLAlchemy>=2.0,<2.1
pydantic[email]>=2.9,<3.0      # EmailStr requiere email-validator
python-dotenv>=1.0,<2.0

# --- NLP ---
nltk>=3.9,<4.0                 # análisis VADER; descarga vader_lexicon en runtime

# --- Seguridad / auth ---
passlib[bcrypt]>=1.7.4,<2.0
python-jose[cryptography]>=3.3,<4.0

# --- Driver de base de datos (producción MySQL) ---
# Para SQLite (por defecto en local/test) no se necesita driver extra.
PyMySQL>=1.1,<2.0

# --- Test ---
pytest>=8.0,<9.0
httpx>=0.27                    # usado por starlette.testclient
